<h1>Listing players</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Date of birth</th>
    <th>Sex</th>
    <th>Bio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @players.each do |player| %>
  <tr>
    <td><%= link_to player.name, player %></td>
    <td><%= player.date_of_birth %></td>
    <td><%= player.sex %></td>
    <td><%= player.bio[0..50] %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Player', new_player_path %>
